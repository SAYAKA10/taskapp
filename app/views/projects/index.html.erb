<div class="container">
 <h1 class="list-title">TODO LIST</h1>
 <h2 class="list-2">TODO一覧</h2>


 <ul>
 <% @projects.each do |project| %>
 <li>
  <%= link_to project.title, project_path(project.id),class: "button3"%> (<%= project.tasks.count %> / <%= project.tasks.unfinished.count %>)
  <%= link_to "編集", edit_project_path(project.id) ,class: "button"%>
  <%= link_to "追加", new_project_path ,class: "button4"%>
  <%= link_to"削除", project_path(project.id), method: :delete, data: { confirm: "本当に消しますか?"} ,class: "button2"%>
 </li>
 <% end %>
 </ul>

 <p> 
 <%= link_to 'ログアウト', destroy_user_session_path, method: :delete %>
 </p>
</div>